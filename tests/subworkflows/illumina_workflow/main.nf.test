nextflow_workflow {

    name "Test Workflow ILLUMINA_WORKFLOW"
    script "subworkflows/illumina_workflow/main.nf"
    workflow "ILLUMINA_WORKFLOW"

    test("Should run without failures") {

        when {
            params {
                // define parameters here. Example:
                outdir = "tests/results"
                primer_set = "16S_ILM_ASU184_meat"
            }
            workflow {
                """
                // define inputs of the workflow here. Example:
                // input[0] = file("test-file.txt")
                input[0] = Channel.of([ [sample_id:'Lasagne_clean', single_end:false], [ file("${projectDir}/tests/data/Lasagne_clean_S1_L001_R1_001.fastq.gz"), file("${projectDir}/tests/data/Lasagne_clean_S1_L001_R2_001.fastq.gz") ] ])
                input[1] = Channel.of(file("${projectDir}/assets/primers/16S_ASU184.fasta"))
                """
            }
        }

        then {
            assert workflow.success
            
        }

    }

}
